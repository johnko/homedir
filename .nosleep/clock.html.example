<html lang="en">

<head>
  <title>clock</title>
  <style>
    /* Default styles (e.g., light mode) */
    body {
      background-color: #fff;
      color: #222;
      height: 100%;
      margin: 0;
      overflow: hidden;
      padding: 0;
      width: 100%;
    }

    video#bg {
      top: 0;
      min-height: 100%;
      min-width: 100%;
      opacity: 30%;
      position: fixed;
      left: 0;
      /* left: -50%; // for vertical screen*/
    }

    /* Styles for dark mode */
    @media (prefers-color-scheme: dark) {
      body {
        background-color: #111;
        color: #aaa;
      }

      video#bg {
        opacity: 15% !important;
      }

      video#fg {
        /* important so displaysleep is prevented */
        opacity: 30% !important;
        width: 100%;
        /* important so displaysleep is prevented */
      }
    }

    div {
      font-family: 'Arial Black', 'Arial Rounded MT Bold';
    }

    .floatclear {
      clear: both;
    }

    #overlay {
      left: 0;
      position: absolute;
      top: 0;
      width: 100%;
    }

    #rowday {
      /* centre */
      max-width: fit-content;
      margin-bottom: 0;
      margin-left: auto;
      margin-right: auto;
      margin-top: 3%;
      white-space: preserve nowrap;

      font-size: 32pt;
    }

    #weekday {
      line-height: 130pt;
    }

    #rowclock {
      /* centre */
      max-width: fit-content;
      margin-bottom: 0;
      margin-left: auto;
      margin-right: auto;

      font-size: 120pt;
    }

    #seconds {
      font-size: 60pt !important;
    }

    video#fg {
      position: absolute;
      /* important so displaysleep is prevented */
      opacity: 100%;
      width: 100%;
      /* important so displaysleep is prevented */
    }

    img#creatormicro {
      float: right;
      margin-left: 150px;
    }

    img#diskchart {
      float: right;
      margin-left: 50px;
      margin-top: 20px;
    }
  </style>
</head>

<body><video id="bg" autoplay="true" loop="true" muted="true" src="file://$NO_SLEEP_VIDEO_FILE"></video><div id="overlay"><div id="rowday"><img id="creatormicro" src="./creatormicro.png"/><span id="weekday"></span><br/><span id="day"></span></div><br class="floatclear" /><div id="rowclock"><img id="diskchart" src="./diskchart.svg"/><span id="clock"></span></div></div><video id="fg" autoplay="true" loop="true" muted="true" src="file://$NO_SLEEP_VIDEO_FILE"></video>
<script>
  function detectBrave() {
    if (typeof navigator.brave !== 'undefined' && typeof navigator.brave.isBrave === 'function') {
      return navigator.brave.isBrave();
    }
    return false;
  }

  const ONE_SECOND = 1000;
  const ONE_MINUTE = 60 * ONE_SECOND;
  const ONE_HOUR = 60 * ONE_MINUTE;

  setInterval(() => {
    let weekday = document.getElementById('weekday');
    let now = new Date();
    if (detectBrave()) {
     weekday.style.fontWeight = 'bold';
    }
    weekday.innerHTML = now.toLocaleDateString('en-US', {
      weekday: "long"
    });
  }, ONE_SECOND);

  setInterval(() => {
    let day = document.getElementById('day');
    let now = new Date();
    if (detectBrave()) {
     day.style.fontWeight = 'bold';
    }
    day.innerHTML = now.toLocaleDateString('en-US', {
      year: "numeric",
      month: "short",
      day: "numeric",
    });
  }, ONE_SECOND);

  setInterval(() => {
    let clock = document.getElementById('clock');
    let now = new Date();
    let hourMinute = now.toLocaleTimeString('en-US', {
      hour: '2-digit',
      minute: '2-digit',
      hour12: false
    });
    seconds = document.createElement('span');
    seconds.id = 'seconds';
    seconds.innerHTML = now.toLocaleTimeString('en-US', {
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit',
      hour12: false
    }).replace(hourMinute, '');
    if (detectBrave()) {
      clock.style.fontWeight = 'bold';
    }
    clock.innerHTML = hourMinute + '' + seconds.outerHTML;
    let fg = document.getElementById('fg');
    fg.style.top = (clock.offsetTop + clock.offsetHeight + 10) + 'px';
    // detect if fg is between clock offsetTop and offsetHeight
    if (fg.offsetTop < clock.offsetTop + clock.offsetHeight) {
      // if so then hide the fg
      fg.style.visibility = 'hidden';
    } else {
      fg.style.visibility = 'visible';
    }
  }, ONE_SECOND);

  setTimeout(() => {
    window.location.reload();
  }, 5 * ONE_HOUR);
</script>
</body>

</html>
